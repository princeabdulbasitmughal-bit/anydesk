import { EmptyResearchState, ExperimentGate, PageHeading } from "@/components/ResearchShell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { useResearch } from "@/contexts/ResearchContext";
import { trpc } from "@/lib/trpc";
import { Bold, Download, FileText, Italic, List, ListOrdered, Loader2, Save } from "lucide-react";
import { useEffect, useRef, useState } from "react";
import { toast } from "sonner";

export default function Findings() {
  const { selectedExperimentId } = useResearch();
  const finding = trpc.research.findings.get.useQuery({ experimentId: selectedExperimentId ?? 1 }, { enabled: Boolean(selectedExperimentId) });
  const reports = trpc.research.reports.list.useQuery({ experimentId: selectedExperimentId ?? 1 }, { enabled: Boolean(selectedExperimentId) });
  const [title, setTitle] = useState("Research findings"); const editor = useRef<HTMLDivElement>(null);
  useEffect(() => { if (finding.data) { setTitle(finding.data.title); if (editor.current) editor.current.innerHTML = finding.data.content; } }, [finding.data]);
  const save = trpc.research.findings.save.useMutation({ onSuccess: () => { finding.refetch(); toast.success("Findings saved"); }, onError: error => toast.error(error.message) });
  const exportReport = trpc.research.reports.export.useMutation({ onSuccess: async output => { await reports.refetch(); window.open(output.url, "_blank", "noopener,noreferrer"); toast.success(`${output.format.toUpperCase()} report exported`); }, onError: error => toast.error(error.message) });
  const doSave = () => { if (selectedExperimentId) save.mutate({ experimentId: selectedExperimentId, title, content: editor.current?.innerHTML || "" }); };
  const format = (command: string) => { editor.current?.focus(); document.execCommand(command, false); };
  return <><PageHeading eyebrow="Scientific record" title="Research findings" description="Capture interpretable notes and analysis decisions in a rich-text research record, then export a structured Markdown or PDF report." /><ExperimentGate><div className="grid gap-5 xl:grid-cols-[1.5fr_0.7fr]"><section className="rounded-2xl border border-white/8 bg-slate-900/40 p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><Input value={title} onChange={event => setTitle(event.target.value)} aria-label="Findings title" className="max-w-md border-white/10 bg-slate-950/50 text-lg font-semibold text-white" /><Button onClick={doSave} disabled={save.isPending} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">{save.isPending ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : <Save className="mr-2 h-4 w-4" />}Save findings</Button></div><div className="mt-5 flex flex-wrap gap-1 border-y border-white/7 py-2"><Button type="button" variant="ghost" size="icon" aria-label="Bold" onClick={() => format("bold")} className="h-8 w-8 text-slate-300 hover:bg-white/5 hover:text-white"><Bold className="h-4 w-4" /></Button><Button type="button" variant="ghost" size="icon" aria-label="Italic" onClick={() => format("italic")} className="h-8 w-8 text-slate-300 hover:bg-white/5 hover:text-white"><Italic className="h-4 w-4" /></Button><Button type="button" variant="ghost" size="icon" aria-label="Bulleted list" onClick={() => format("insertUnorderedList")} className="h-8 w-8 text-slate-300 hover:bg-white/5 hover:text-white"><List className="h-4 w-4" /></Button><Button type="button" variant="ghost" size="icon" aria-label="Numbered list" onClick={() => format("insertOrderedList")} className="h-8 w-8 text-slate-300 hover:bg-white/5 hover:text-white"><ListOrdered className="h-4 w-4" /></Button></div><div ref={editor} contentEditable suppressContentEditableWarning data-placeholder="Record observations, interpretation, limitations, and next steps…" className="rich-editor mt-5 min-h-[420px] rounded-xl border border-white/8 bg-slate-950/35 p-5 text-sm leading-7 text-slate-200 outline-none transition focus:border-cyan-300/50" /></section><aside className="rounded-2xl border border-white/8 bg-slate-900/40 p-6"><FileText className="h-5 w-5 text-cyan-300" /><h2 className="mt-4 font-display text-xl font-semibold text-white">Report exports</h2><p className="mt-2 text-sm leading-6 text-slate-400">Exports include the experiment summary, findings, and recorded run history.</p><div className="mt-5 grid gap-2"><Button disabled={exportReport.isPending} onClick={() => selectedExperimentId && exportReport.mutate({ experimentId: selectedExperimentId, format: "markdown" })} variant="outline" className="justify-between border-white/10 text-slate-200 hover:bg-white/5 hover:text-white">Markdown <Download className="h-4 w-4" /></Button><Button disabled={exportReport.isPending} onClick={() => selectedExperimentId && exportReport.mutate({ experimentId: selectedExperimentId, format: "pdf" })} variant="outline" className="justify-between border-white/10 text-slate-200 hover:bg-white/5 hover:text-white">PDF <Download className="h-4 w-4" /></Button></div><div className="mt-7 border-t border-white/7 pt-5"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-slate-500">Stored exports</p>{reports.data?.length ? <div className="mt-3 space-y-2">{reports.data.map(report => <a key={report.id} href={report.fileUrl} className="flex items-center justify-between rounded-lg bg-white/[0.035] px-3 py-2 text-xs text-slate-300 transition hover:bg-white/[0.07]"><span>{report.format.toUpperCase()}</span><Download className="h-3.5 w-3.5 text-cyan-300" /></a>)}</div> : <p className="mt-3 text-xs leading-5 text-slate-500">No report exports have been created.</p>}</div></aside></div></ExperimentGate></>;
}
